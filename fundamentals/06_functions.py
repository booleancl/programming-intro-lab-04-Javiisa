# Una funciòn es un grupo de sentencias con un nombre que realizan una computaciòn
# primero uno define una funciòn y luego de "llama" o "ejecuta"esa funciòn

# python viene con muchas funciones listas para usas.
# solo hay que llamarlas y ejecitarlas
print("hola charlotte")

#la mayoria e las funciones entregan un valor es decir, nos devuelve el resultado.ejemplo:

str_num ='32' #Esto es un string que parece numero
real_num =int(str_num)#Esta funciòn se tranforma a entero
print(type(real_num))

float_num =3.9999
int_num = int(float_num)
print(int_num)
#La siguiente linea es un error
#int ("hola inmundo)"

print(float('32'))#esta funciòn estrega un float
print(str(3.1415))#Esta funciòn entrga un str

#composiciòn de funciones :Anidar funciones

print(str(3.1415))
